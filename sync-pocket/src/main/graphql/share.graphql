mutation CreateShareLink($target: ValidUrl!, $context: ShareContextInput) @figment(priority: "REMOTE") {
    createShareLink(target: $target, context: $context) {
        shareUrl
        slug
        targetUrl
    }
}
