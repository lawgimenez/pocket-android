query getUnleashAssignments($context: UnleashContext!) {
    assignments: getUnleashAssignments(context: $context) {
        assignments {
            name
            assigned
            variant
            payload
        }
    }
}
